const form=document.querySelector('#project');const out=document.querySelector('#output');form.addEventListener('submit',async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(form));d.collectionSites=d.collectionSites.split(',').map(x=>x.trim()).filter(Boolean);d.idempotencyKey=crypto.randomUUID();const r=await fetch('/api/projects',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(d)});out.textContent=JSON.stringify(await r.json(),null,2)})
